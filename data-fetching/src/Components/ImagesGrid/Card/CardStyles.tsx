import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  card: {
    paddingTop: '56.25%',
    position: 'relative'
  },
  grid: {
    position: 'absolute',
    top: '0',
    left: '0',
    height: '100%'
  }
});
